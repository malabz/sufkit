# Maximum-right-match results

These are **maximum right matches**, not MEMs: left maximality is not implied. The table contains baseline, LCP, CHILD, suffix-link, full and sampled-SA variants present in completed stages. The headline comparison is taken from audited full/mixed streaming results (3 repetitions); the unfinished dedicated headline repetition is excluded.
