# Scenario coverage

Quick covers `mixed`, `balanced`, `gc-skewed`, `repeat-rich`, `n-islands`, and `many-contig`. Smoke covers `mixed`. Standard and full were shortened to representative `mixed` experiments and must not be interpreted as complete six-scenario scans. No real-genome experiment is claimed in this snapshot.
